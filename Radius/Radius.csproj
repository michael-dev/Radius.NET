<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net6.0</TargetFrameworks>
    <AssemblyTitle>Radius.NET</AssemblyTitle>
    <Company>Front Porch Inc.</Company>
    <Product>Radius.NET</Product>
    <Description>Radius client for .NET</Description>
    <Copyright>Copyright 2016</Copyright>
    <Version>1.0.0.0</Version>
    <AssemblyVersion>1.0.0.0</AssemblyVersion>
    <FileVersion>1.0.0.0</FileVersion>
    <OutputPath>bin\$(Configuration)\</OutputPath>
    <Authors>Front Porch Inc.</Authors>
    <PackageLicenseUrl>http://opensource.org/licenses/MIT</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/Frederic-HEBERT/Radius.NET</PackageProjectUrl>
    <PackageReleaseNotes>
      Added Server-Status support
      Added a Ping() method to the RadiusClient to allow easy sending of Server-Status packets
      ** Potentially Incompatible Change ** No longer call VerifyAuthenticator inside of SendAndReceivePacket which results in packets being returned that should fail authentication.  Callers should do this instead once the response packet has been received from the RADIUS server.
    </PackageReleaseNotes>
    <PackageTags>Radius .NET</PackageTags>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugType>full</DebugType>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
  </PropertyGroup>
</Project>